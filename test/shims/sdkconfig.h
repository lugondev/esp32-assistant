#pragma once
// Host-test stub: no Kconfig options are defined, so `#if CONFIG_FOO` checks
// evaluate falsy (0) automatically, matching real-hardware defaults for
// anything the host build doesn't override.
